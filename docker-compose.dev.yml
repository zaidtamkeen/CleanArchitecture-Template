version: "3.9"
services:
  api:
    build:
      context: .
      dockerfile: src/Web/Api/Dockerfile
    ports:
      - "8080:8080"
    env_file: .env
    depends_on:
      - redis
  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
